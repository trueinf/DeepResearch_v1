[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  # Skip secrets scanning for documentation files (they contain example keys only)
  SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES = "true"